// Actions

// Action Creators

// reducer
export default (() => {
  const initState = {
  }
  return (state = initState, action) => {
    switch (action.type) {
      default:
        return state
    }
  }
})()
